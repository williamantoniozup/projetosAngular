var minhaVar = 'minha variavel';

function minhaFunc(x, y) {
    return x + y;
}

// ES6 ou ES 2015
let num = 2;
const PI = 3.14;

var numeros = [1,2,3];
numeros.map(function(valor){
    return valor * 2;
});

numeros.map(valor => valor * 4);

class Matematica {
    soma(x,y){
        return x+y;
    }
}

var n1: string = 'asdasa';  // --> tipagem de variavel;
var n2 = 4;






/*
 ECMAS2015 ==> transpile, compilar o ES6 em vanilla puro

    to transpile -> tsc namefile.ts


 */